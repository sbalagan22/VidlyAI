import { FetchedTranscriptSnippet } from './models';
/**
 * Parser for YouTube transcript XML data
 */
export declare class TranscriptParser {
    private readonly preserveFormatting;
    private readonly htmlRegex;
    constructor(preserveFormatting?: boolean);
    /**
     * Parse raw transcript XML data into snippets
     */
    parse(rawData: string): Promise<FetchedTranscriptSnippet[]>;
    /**
     * Parse XML string into object
     */
    private parseXml;
    /**
     * Clean text by removing unwanted HTML tags
     */
    private cleanText;
    /**
     * Get HTML regex based on formatting preservation setting
     */
    private getHtmlRegex;
}
/**
 * Utility function to parse transcript XML
 */
export declare function parseTranscriptXml(rawData: string, preserveFormatting?: boolean): Promise<FetchedTranscriptSnippet[]>;
//# sourceMappingURL=parser.d.ts.map